#!/usr/bin/env node
require('source-map-support').install();
var Controller = require('../').lib.Control;

new Controller().start({ config: 'config.js', activity: true, decider: true },
  function(err, controller) { });
